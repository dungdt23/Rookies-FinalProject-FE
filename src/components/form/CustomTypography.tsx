import {styled} from '@mui/system';
import {Typography} from '@mui/material';

export const FormTitle = styled(Typography)(({ theme }) => ({
    fontWeight: 'bold',
    color: 'red',
    fontSize: '1.2em',
    textAlign: 'center',
    marginBottom: '20px',
}));
const CustomTypography: FC = () => {
}
export default CustomTypography;