import { styled } from '@mui/system';
import { Box } from '@mui/material';

const CustomGenderField = styled(Box)(({ theme }) => ({
    display: 'flex',
    alignItems: 'center',
}));

export default CustomGenderField;
