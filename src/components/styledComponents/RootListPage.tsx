import { Box, styled } from "@mui/material";

export const RootListBox = styled(Box)(() => ({
    display: "flex",
    flexDirection: "column",
    minWidth: "30rem",
    width: "100%",
    maxHeight: "100%",
    p: 2,
}));